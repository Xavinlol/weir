global:
  scrape_interval: 10s

# Whichever profile is not running simply fails to resolve.
scrape_configs:
  - job_name: weir
    static_configs:
      - targets: ['weir:9000']

  - job_name: weir-cluster
    dns_sd_configs:
      - names: ['weir-cluster']
        type: A
        port: 9000
